"""Legacy stub retained for backward compatibility.

The project now uses ``pyproject.toml`` for all build and packaging
configuration. This file remains only to prevent packaging tools that expect
``setup.py`` from failing with a missing-file error.
"""

from setuptools import setup

setup()
